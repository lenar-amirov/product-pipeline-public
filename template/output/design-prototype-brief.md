# Design Prototype Brief

> Generated by `/sketch-solution` (Step 8a). Tool-agnostic — adapt any field.
> Hand off to your renderer of choice; default is Claude Design (claude.ai/design).
> When you bring an exported prototype back, say `design ready: <link>` to close the gate.

**Renderer**: {{design_config.renderer}}        <!-- claude-design | html | figma | text -->
**Platform**: {{design_config.platform}}        <!-- mobile | desktop | responsive -->
**Fidelity**: {{design_config.fidelity}}         <!-- lofi (concept test) | hifi (Gate report) -->
**Design system**: {{design_config.design_system}}   <!-- generate | tokens-file | figma-lib -->  [to be validated]
**Scope**: {{design_config.scope}}               <!-- single (top hypothesis) | flow (top 2–3 screens) -->
**Variants**: {{design_config.variants}}

---

## Screens to prototype

| Screen | Solves hypothesis | UI patterns | Notes |
|--------|-------------------|-------------|-------|
| [01_name] | S[N] | [pattern from ui-pattern-library] | [state / edge case] |

---

## Prompt for {{design_config.renderer}}

[Generated from `output/solution-hypotheses.md` + design tokens. Edit any block before handing off.]

- **Goal**: prototype the solution for hypothesis S[N] — [one line].
- **Tokens**: [colors / spacing / typography — from `ui-pattern-library` or your `design/tokens.json`].
- **Flow**: [entry → screens → success state].
- **Acceptance**: [what the clickable prototype must demonstrate].

---

## Handoff (optional, encouraged — does not block the pipeline)

1. Step 8a already produced a draft prototype at `design/prototype-draft.html` (self-contained HTML on the tokens above).
2. Take this brief + the draft into your renderer (Claude Design: *point Claude at this prototype*, then web-capture / inline knobs / share links).
3. Export a standalone prototype back to `design/prototype.html` — it supersedes the draft as the "current" prototype.
4. Say `design ready: <link>` → closes `pending.design_export`, records the link in `solution-sketch.md`.

If you skip the handoff, the draft remains the current prototype — the pipeline degrades gracefully.
